#include<stdio.h>
int main()
{
	int a,b;
	scanf("%d%d",&a,&b);
	printf("the addition of two numbers is %d ,the multiplication is %d ,the substraction is %d ,the division is %d",a+b,a*b,a-b,a/b);
	return 0;
}
